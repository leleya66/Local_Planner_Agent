"""Modular workflow entry points for Agent 4.

The modules in this package provide categorized access to the legacy
agent_workflow_improved implementation. They are intentionally thin first, so
the public boundaries can stabilize before function bodies are moved.
"""
